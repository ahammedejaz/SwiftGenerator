export default function SecurityPage() {
  return <main className="mx-auto max-w-4xl px-6 py-10"><p className="text-sm font-semibold uppercase tracking-widest text-teal-700">Security posture</p><h1 className="mt-2 text-3xl font-semibold">Protected Financial-Data Boundary</h1><div className="mt-8 grid gap-5 md:grid-cols-2">{[{title:"Identity and access",body:"Server-side sessions, secure cookie controls, CSRF defence, tenant scoping, and least-privilege roles. Production requires OIDC or SAML mode."},{title:"Encrypted values",body:"Each financial field uses an authenticated envelope with a random data key wrapped by a server-side key. Key material never reaches the browser."},{title:"Maker-checker",body:"Authors cannot approve their own production-bound revisions. Any edit increments the revision and invalidates active approval."},{title:"Submission safety",body:"No browser-to-network posting, no arbitrary destinations, production disabled by default, checksum and idempotency enforced."},{title:"AI minimisation",body:"Deterministic forms, Tag Intelligence, composition, validation, downloads, and submission never call the LLM. Unstructured input is tokenised before optional AI use."},{title:"Capability honesty",body:"All 16 authoring families remain PARTIAL until complete authorised SR2026 and client rule packs pass the production coverage gate."}].map((item) => <section key={item.title} className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">{item.title}</h2><p className="mt-2 leading-7 text-slate-600">{item.body}</p></section>)}</div></main>;
}
